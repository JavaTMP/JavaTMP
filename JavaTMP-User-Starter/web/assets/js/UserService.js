/*!
 * JavaTMP index.html JS File.
 */
;
(function ($, window, document, javatmp, undefined) {

    javatmp.modules = javatmp.modules || {};

    var defaults = {

    };

    javatmp.modules.userService = function (options) {
        $.extend(true, defaults, options);
    };

}(jQuery, window, document, window.javatmp));

